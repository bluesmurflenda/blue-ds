# 진행 상태

**재개용 메모다.** 세션이 끊겨도 이것만 읽으면 이어서 할 수 있다.

**규칙은 여기 적지 않는다** — 규칙은 `SCSS.md` 와 `FIGMA.md`, 결정은 `DECISIONS.md` 에 있다.
**아직 안 정한 것은 전부 여기 「남은 일」에 둔다** — `DECISIONS.md` 는 정해진 것만 담는다.
**지금 상태의 세부를 적지 않는다** — 어느 컴포넌트가 어떤지, 개수, 값. 전부 반드시 낡는다.
**검사가 판정할 수 있는 것도 적지 않는다** — `npm run check` 가 매번 새로 낸다.

**고칠 때는 덧붙이지 않는다. 이전 줄을 지우고 바꾼다.**

---

## 지금 하는 일

**넘길 준비가 끝났다.** 컴포넌트 전수가 Figma 와 맞춰졌고, 토큰 SCSS 는 스냅샷에서
생성되며, 코드 규약·값·화면을 각각 도구가 본다(`npm run check` · `npm run check:visual`).

**Figma 와 다시 맞췄다.** 퍼블리시된 변경을 코드가 따라잡았다.
컴포넌트를 다시 손볼 일이 생기면 그건 Figma 가 바뀐 것이므로
`SCSS.md` 「컴포넌트 하나를 끝내는 흐름」으로 돌아간다.
다음은 아래 「Figma 에서 한 번에 고친다」이다.

**제외 대상** — logo · login-header · con · header · side-nav-item.

**배포는 개발자가 담당한다.** 이 저장소는 배포를 준비하지 않는다.

---

## 남은 일

### Figma 에서 한 번에 고친다

**배포 뒤에 몰아서 한다.** 하나씩 고치면 퍼블리시 → 스냅샷 → 빌드 한 바퀴가 매번 돈다.

- Variables 문서 페이지의 초점 링 행이 아직 Primitive 그룹 자리에 있다 — 토큰은 Theme 으로 옮겼다
- 목록 항목·목록 패널의 반경에 맞는 Shape 토큰이 없다 — 각진·Pill 모드에서 걸린다
- Con · Card 에 disabled 표현이 없다 — Figma 에 상태를 넣을지, 코드에서 흐리게 할지
- 목록 항목에 focus 표시가 없다 — Figma 에 넣을지, 코드에서 링으로 그릴지
- Modal 의 alert 계열에 큰 사이즈가 없다 — 의도인지 확인해야 한다
- Featured Icon 에 info 색이 없다 — 넣을지 그대로 둘지
- Nav 계열의 라이트·다크를 변형 축에 둘지 모드로 옮길지

### 코드에서 정할 것

- **겹침 순서 규칙이 없다** — 저장소 전체에 `z-index` 가 0건이다. 지금은 DOM 순서로 버티지만
  떠 있는 것(드롭다운 패널·툴팁·모달)이 늘면 반드시 걸린다. 층위를 먼저 정해야 한다
- 드롭다운 방향 옵션(위·왼쪽·오른쪽) — 패널을 흐름에서 빼 절대 배치로 바꿔야 하고,
  그 전에 겹침 순서가 정해져야 한다. CSS 만 배포하므로 자동 뒤집기는 없다
- 체크박스·툴팁 모서리 반경이 하드값이다 — 토큰으로 뺄지 그대로 둘지
- 눈금 사이 절반 값을 스케일에 넣은 것을 ADR 로 남길지 — Toggle · Featured Icon · Tabs ·
  Modal 이 실제로 그 값을 물고 있어 이제 되돌리기 어렵다
- 토큰 관리 방향을 Git 기준으로 바꿀지 정한다
- **치수와 변형 일관성을 보는 검사가 없다** — 값이 토큰을 무는지는 보지만
  크기가 맞는지, 변형끼리 값이 어긋나지 않는지는 아직 사람이 Figma 를 읽어 대조한다
