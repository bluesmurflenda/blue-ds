# 진행 상태

**재개용 메모다.** 세션이 끊겨도 이것만 읽으면 이어서 할 수 있다.

**규칙은 여기 적지 않는다** — 규칙은 `SCSS.md` 와 `FIGMA.md`, 결정은 `DECISIONS.md` 에 있다.
**지금 상태의 세부를 적지 않는다** — 어느 컴포넌트가 어떤지, 개수, 값. 전부 반드시 낡는다.
**검사가 판정할 수 있는 것도 적지 않는다** — `npm run check` 가 매번 새로 낸다.

**고칠 때는 덧붙이지 않는다. 이전 줄을 지우고 바꾼다.**

---

## 지금 하는 일

**9단계 — 검사와 도구를 정리한다.** 8단계까지 끝났다 — Figma 컴포넌트 세트 전수가
SCSS 와 대조됐고, 없던 것은 만들었다. 무엇이 맞고 틀렸는지는 git 로그에 있다.

**여기서부터는 아래 「검사·도구」 순서를 따른다.** 컴포넌트를 다시 손볼 일이 생기면
그건 Figma 가 바뀐 것이므로 `SCSS.md` 「컴포넌트 하나를 끝내는 흐름」으로 돌아간다.

**제외 대상** — logo · login-header · con · header · side-nav-item.

**배포는 개발자가 담당한다.** 이 저장소는 배포를 준비하지 않는다.

---

## 검사·도구

**앞이 뒤의 전제다.**

1. CI 를 실제로 돌게 만든다 — 저장소 비밀값이 없어 Figma 노드 검사가 조용히 건너뛴다
2. 안 덮이는 부분만 검사로 만든다 — 치수, 그리고 변형끼리 값이 일관된지.
   **지금 둘 다 보는 검사가 없어서 사람이 Figma 를 읽어 대조한다**
3. Figma `codeSyntax` 를 `var(--bds-*)` 로 쓴다
4. 토큰 관리 방향을 Git 기준으로 바꿀지 정한다

---

## 남은 일

- 목록 항목·목록 패널의 반경에 맞는 Shape 토큰이 없다 — 각진·Pill 모드를 쓸 때 걸린다
- **아직 사람이 눈으로 확인하지 않았다** — 커서도 나도 브라우저가 없다.
  컴포넌트 대부분에서 간격·반경·테두리가 움직였으니 문서 사이트를 한 번 훑어야 한다
- 눈금 사이 절반 값을 스케일에 넣은 것을 ADR 로 남길지 — Toggle · Featured Icon · Tabs ·
  Modal 이 실제로 그 값을 물고 있어 이제 되돌리기 어렵다
- ADR-019 를 비었다고 정리할지
- Nav 계열의 라이트·다크를 변형 축에 둘지 모드로 옮길지
- `DECISIONS.md` 미결 항목 중 선례가 생긴 것들의 확정
